<h1><%= @my_article.title %></h1>
<p><%= @my_article.body %></p>

<%= link_to 'Edit', edit_my_article_path(@my_article) %> |
<%= button_to "Delete", @my_article, method: :delete, data:{
    turbo_confirm: "Are you sure?"} %>
    

<%= link_to 'Back to Articles', my_articles_path %>
