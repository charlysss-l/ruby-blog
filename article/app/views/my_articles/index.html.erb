<%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete, confirm: "Are you sure?" } %>

<h1>My Articles</h1>

<!-- Add links for new article and logout -->
<div class="actions">
  <%= link_to "Add New Article", new_my_article_path %> |
</div>

<div id="my_articles">
  <% @my_articles.each do |article| %>
    <div>
      <%= link_to article.title, my_article_path(article) %>
    </div>
  <% end %>
</div>
